const lines = [ "function findAvailableRoom(constraints) {", " const rooms = getRooms();", " return solve(rooms, constraints);", "}", ]; export default function CodeWindow() { return (
<div className="bg-surface border border-line md:mt-2">
    <div className="flex items-center gap-2 px-4 py-3 border-b border-line">
        <span className="w-2 h-2 rounded-full bg-accent" />
        <span className="w-2 h-2 rounded-full bg-line" />
        <span className="w-2 h-2 rounded-full bg-line" />
        <span className="font-mono text-xs text-ink-faint ml-2">scheduler.ts</span>
    </div>

    <div className="px-5 py-6 font-mono text-[13px] leading-[1.9] text-ink">
        {lines.map((line, i) => (
        <div key={i} className="whitespace-pre opacity-0" style={{ animation: "fadeUp 0.5s ease-out forwards", animationDelay: `${i * 120 + 200}ms`, }}>
            {line}
        </div>
        ))}
        <div className="text-ink-faint mt-3 opacity-0" style={{ animation: "fadeUp 0.5s ease-out forwards", animationDelay: `${lines.length * 120 + 320}ms`, }}>
            {"// websocket sync — live"}
        </div>
    </div>
</div>
); }